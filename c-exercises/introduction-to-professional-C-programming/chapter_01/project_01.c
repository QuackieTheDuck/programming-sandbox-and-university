#include <stdio.h>
#include <stdlib.h>
#include <conio.h>      // add getch() command

int main(void){
    puts("Witaj, swiecie!");
    getch();    // makes program wit for any key in order to continue
    return EXIT_SUCCESS;
}